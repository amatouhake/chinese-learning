# Generate a long random value and keep the real .dev.vars file private.
ATTEMPT_WRITE_TOKEN=replace-with-a-long-random-secret
# Wrangler reads this only for local development. The Worker also requires a loopback hostname.
LOCAL_STUDY_BYPASS=true
